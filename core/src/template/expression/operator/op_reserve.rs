// =============================================================================
// OpReserve
// =============================================================================

// Types

#[allow(dead_code)]
#[derive(Clone, Debug, PartialEq)]
pub enum OpReserve {
    Equals,
    Comma,
    Exclamation,
    At,
    Pipe,
}
